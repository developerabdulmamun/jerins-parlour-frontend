import Login from "@/components/ui/Accounts/Login";
import React from "react";

export const metadata = {
  title: "Login | Jerin's Parlour",
  description: "Beauty saloon for every women",
};

const LoginPage = () => {
  return (
    <div>
      <Login />
    </div>
  );
};

export default LoginPage;
